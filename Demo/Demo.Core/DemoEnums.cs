namespace Demo.Core
{
    public class DemoEnums
    {
        public enum Status
        {
            Block = -1,
            Inactive = 0,
            Active = 1,
        }

        public enum Role
        {
            Admin = 0,
            User = 1
        }

        public enum FileUploadType
        {
            Photo = 0
        }
    }
}
